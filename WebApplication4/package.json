{
  "name": "backoffice",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install",
    "preinstall": "npm install -g bower --force"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.8.14",
    "jquery": "~3.6.3"
  }
}

